#include <stdio.h>

int main(int argc, char const *argv[]) {
  /* code */
  // get order input
  // look up data in file
  // send data to ship label api
  // format data to label

  printf("Enter lookup ID or press n to enter manually.\n");


  return 0;
}

// gcc main.c resource.c -o output
