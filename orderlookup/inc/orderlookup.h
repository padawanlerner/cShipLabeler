int test();
// some function where you input an order id and return a struct full of order data.
struct Order lookupOrder(char *lookupID);

// some other function that scanfs order data from the user
struct Order enterOrder();
